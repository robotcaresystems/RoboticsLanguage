node(
  name:"example functions",
  definitions:block(
    y ∈ Integers(bits:16),
    x ∈ Integers,
    z ∈ Reals,
    w ∈ Reals(bits:64)
    
    ## defineFunction(
      name("test"),
      arguments( x ∈ Integers(bits:16), y ∈ Integers, z:1 ∈ Integers),
      returnType(Integers),
      content(
        return(x+y+z)
        )
      )
      ##
    )
)
