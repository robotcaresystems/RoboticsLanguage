node(
  name:'example temporal logic',

  definitions: block(
    # a signal
    x ∈ Signals(Reals, rosTopic:'/temporal_logic/x'),

    y ∈ Signals(Reals, rosTopic:'/temporal_logic/y')

  ),

  events: block(
    if( □[0,1](¬◇[0,2](◇[0,1](x > 0))),
        print('x positive')
      )
    ,

    if(◇(x == 0),
      print('x = 0')
      )
    )
)
